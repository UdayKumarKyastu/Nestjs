import { DateAttribute } from '../base-attributes/date-attribute'

export class LiveToAttribute extends DateAttribute {
  static readonly COMMERCE_TOOLS_ATTR_NAME = 'liveTo'

  private _tag = 'LiveToAttribute'
}
